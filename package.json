{
  "name": "@mapsindoors/midt",
  "version": "18.1.0",
  "description": "Design Tokens for MapsIndoors",
  "author": "MapsPeople A/S",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/mapspeople/midt.git"
  },
  "keywords": [
    "mapsindoors",
    "design",
    "tokens",
    "system"
  ],
  "sass": "mixins/_index.scss",
  "style": "midt.css",
  "scripts": {
    "build": "npm run clean && style-dictionary build && npm run sass",
    "clean": "style-dictionary clean",
    "sass-watch": "sass --watch midt.scss:midt.css --no-source-map",
    "sass": "sass midt.scss:midt.css --no-source-map",
    "watch": "npm run build && chokidar \"properties/**/*.json\" -c \"npm run build\"",
    "test": "echo \"no tests lol\" && exit 1"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "np": "^7.6.3",
    "sass": "^1.57.1",
    "style-dictionary": "^3.7.2"
  }
}